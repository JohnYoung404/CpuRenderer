#include "color.h"

const CPURenderer::Color CPURenderer::Color::red({ 255, 0, 0, 255 });
const CPURenderer::Color CPURenderer::Color::green({ 0, 255, 0, 255 });
const CPURenderer::Color CPURenderer::Color::blue({ 0, 0, 255, 255 });