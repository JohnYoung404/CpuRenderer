#include "color.h"

const CPURenderer::Color CPURenderer::Color::red({ 255, 0, 0 });
const CPURenderer::Color CPURenderer::Color::green({ 0, 255, 0 });
const CPURenderer::Color CPURenderer::Color::blue({ 0, 0, 255 });
const CPURenderer::Color CPURenderer::Color::black({ 0, 0, 0 });
const CPURenderer::Color CPURenderer::Color::white({ 255, 255, 255 });
const CPURenderer::Color CPURenderer::Color::yellow({ 255, 255, 0 });
const CPURenderer::Color CPURenderer::Color::sky_blue({ 0, 255, 255 });